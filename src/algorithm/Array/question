原地哈希：（主要是将数组当作哈希表可以判断数组中某些元素是否出现过，如将数值为i的数映射到数组下标为i - 1 的位置）
原地修改：就地的修改数值为i的数所对应数组下标为i - 1的位置的值（如改为负数等标识数值为i出现过）
原地哈希是会出现数组值交换操作，原地修改是出现对指定数组索引处的值进行标记的操作
特点是数据出现在一定的范围内，如0,n(n为数组长度)
题目特点是能否用线性时间复杂度完成并且只用常量级别的空间，这样一般实现方法需要修改原数组
出现的题目有：剑指offer3、leetcode41、268、442、448、287. 寻找重复数（不太符合题目要求，题目要求不能修改输入）

几道模拟路径题：54. 螺旋矩阵、59. 螺旋矩阵 II、498. 对角线遍历、6. Z 字形变换（字符串区） 前三题均为二维数组（矩阵模拟）
矩阵：73. 矩阵置零

与数字有关的模拟题（需要拆分数字int值做相加相乘等等操作或者是反转等等）：
7. 整数反转、8. 字符串转换整数 (atoi)、165. 比较版本号、166. 分数到小数、168. Excel表列名称、43. 字符串相乘

涉及到区间（二维数组）的内容：
56. 合并区间、57. 合并区间 II、252. 会议室I、253. 会议室II

tip:如果一次遍历数组或矩阵的题找不到思路可以试试两次遍历数组或矩阵（一遍标记，一遍筛选...）
或者试着反向去遍历数组，也许这样就是解题的思路