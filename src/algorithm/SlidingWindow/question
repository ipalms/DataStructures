不甚好理解的滑动窗口：
424. 替换后的最长重复字符（这个窗口长度只会不变或者变大）
1004、最长的1串（也可以使用424做法，这样的话这个窗口长度只会不变或者变大）



维护一段数据的最大值或者最小值可以采用：
单调队列(需要维护 Deque--LinkedList)、堆（优先队列 PriorityQueue）
红黑树（TreeMap）、或者对数据排序

220. 存在重复元素 III、1438. 绝对差不超过限制的最长连续子数组（这两题都是考察一段数据中的数据差值关系）
239. 滑动窗口最大值（与220一样都是维护固定窗口大小）
以上三题均可以采取红黑树（TreeSet）作为维护数据区间关系的集合
239、1438题也可以采用单调队列维护一段数据最大值最小值
