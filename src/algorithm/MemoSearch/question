记忆化搜索：
记忆化即记忆化搜索，也叫记忆化递归，其实就是拆分子问题的时候，发现有很多重复子问题，然后再求解它们以后记录下来。
以后再遇到要求解同样规模的子问题的时候，直接读取答案。

空间换时间的一种方式，利用空间储存好之前已经做过的结果，后面再遇到就不用再做计算，直接拿来用就好了。
比如说某一点已经计算得到这点的最大路径了，后面有其它点计算最大路径时经过该点并想要得知该点的最大路径，
就直接从相应的记忆存储里取出来，不用再去计算该点的最大路径。

记忆化集合可以使用二维数组（如果输入的是二维数组），也可以使用HashMap存储记忆化数据。
记忆化搜索一般搭配着深度优先(dfs)进行

一些例题：
337. 打家劫舍 III、120. 三角形最小路径和、139. 单词拆分（前面这三题都在动态规划区，都可以使用记忆化搜索但均非最优）
329. 矩阵中的最长递增路径（记忆化最优）